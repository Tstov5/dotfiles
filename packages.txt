# --- Compositor / Display Manager ---
niri
ly

# --- Terminal / launcher / lock / file manager---
kitty
fuzzel
gtklock
thunar

# --- Wayland utilities (spawned at startup in niri/config.kdl) ---
swaybg
wl-clipboard
cliphist

# --- Audio / media / brightness (niri keybinds, audio-switcher.sh) ---
pipewire
pipewire-pulse
pipewire-alsa
wireplumber
playerctl
brightnessctl

# --- Network & Bluetooth (wayle bar modules, bluetooth-toggle.sh) ---
networkmanager
bluez
bluez-utils

# --- Font (used by kitty, fuzzel, wayle, wlogout) ---
ttf-jetbrains-mono-nerd

# --- Keyring / secrets ---
gnome-keyring
libsecret
python-keyring

# --- Runtime: Node.js 20+ (required by the Cline CLI) ---
nodejs
npm

# --- AUR (install with yay, not pacman) ---
# wayle-bin is the prebuilt binary. The source package (wayle) compiles a large
# Rust workspace and fails on fresh systems where rustup has no toolchain
# installed (RUSTUP_TOOLCHAIN=stable -> "toolchain 'stable' is not installed").
wayle-bin
wlogout
# localsend is a source build (Rust + Flutter via the `fvm` AUR makedepend),
# so it takes several minutes to compile. Use localsend-bin for a prebuilt
# binary if you prefer a faster install.
localsend
zen-browser-bin
# shelly is a source build using zig; it conflicts with shelly-bin and
# shelly-git, so choose only one. shelly-bin avoids the compilation step.
shelly
